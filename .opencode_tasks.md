# MEGA PAPER MASTER — Transport Operating System — Task Checklist

Estratto da `C:\Users\Asus\Downloads\TRANSPORT SaaS paper.md` (75 sezioni, roadmap §65).
Legenda: `[x]` verificato | `[ ]` da fare | `[FAILED]` bloccato (motivo in parentesi) | `[n]` nella nota / non-loppabile.

## FASE 0 — FOUNDATION (§65)
- [x] Repository e stack (Next.js 16 + React 19 + Prisma + Supabase + NextAuth v5 + Stripe + UploadThing)
- [x] Backend / API route handlers
- [x] Frontend (dashboard web)
- [x] Database (Supabase Postgres via pooler)
- [x] Auth (NextAuth v5, credenziali demo `admin@demo.com`/`Demo123!`, `autista@demo.com`/`Demo123!`)
- [x] Company (multi-step registration + KYC upload)
- [x] Roles (UserRole: ADMIN/UFFICIO/AUTISTA/COMMITTENTE)
- [x] Multi-tenancy (tenant proxy `getTenantDb`, `validateTenantAccess`)
- [x] Security (bcrypt hashing, sessioni, autorizzazioni backend, isolamento tenant, audit log, validazione input, upload sicuri)

## FASE 1 — GESTIONALE CORE (§65)
- [x] ACCOUNT (registrazione/accesso)
- [x] ADMIN (dashboard, gestione azienda/utenti)
- [x] MEZZI (CRUD veicoli, stati, documenti, scadenze)
- [x] AUTISTI (gestione utenti autisti, legame a veicolo)
- [x] VIAGGI (CRUD viaggi: luogo ritiro/consegna, date, merce, peso, prezzo/costo, cliente)
- [x] ASSEGNAZIONE (assegnazione veicolo+autista, transizioni stato da ad admin)

## FASE 2 — OPERATIVITÀ (§65)
- [x] PWA (manifest, service worker, icone, installabile, optimizzata per autista)
- [x] DOCUMENTI (upload documenti veicoli, verifica scadenze, alert dashboard)
- [x] T1 — DDT DIGITALE (§18): modello `TripDocument` (foto/firma/documento dal driver), upload dalla PWA autista al completamento, lista/logistica visibile ad ADMIN (riduce il tempo tra consegna e documentazione)
- [x] T2 — TRACKING & STATO CONSEGNA (§53): modello `TrackingEvent` (posizione/stato consegna aggiornati dall'autista), timeline visibile ad ADMIN; stato consegna + informazioni operative
- [x] T3 — NOTIFICHE: pagina notifiche con alert attive (scadenze documenti, KYC da valutare, viaggi da assegnare, prelievi da processare) + contatore nella nav

## FASE 3 — API (§65)
- [x] Endpoint UploadThing (KYC, VehicleDocument, DDT photo) — `app/api/uploadthing/core.ts`
- [x] T4 — INTEGRAZIONI API (§19/§20/§61): modelli `ExternalIntegration`+`ApiConnection`, workflow ADMIN → Integrazioni → Collega API → Autorizzazione → Test → Sync; credenziali cifrate (AES-256-GCM); provider simulati (GPS/TMS/ERP/TACHIGRAFO/BORSA-CARICHI/CARBURANTE)
- [x] T5 — GPS/TELEMETRIA (mock): coperto da TrackingEvent (posizione/stato) + catalogo/integrazione GPS; posizione simulata del mezzo disponibile per tracking e smart return

## FASE 4 — ABBONAMENTO + VERIFICA (§65)
- [x] SUBSCRIPTION (Stripe webhook, card abbonamento, attivazione TRIAL→ACTIVE dopo verifica KYC)
- [x] VERIFICATION (review documento KYC, `verifiedBy`/`verifiedAt`)
- [x] T6 — VERIFIED ACCOUNT (§21): secondo livello account (API COLLEGATE + ABBONAMENTO + VERIFICA) — criterio computato + badge in sidebar + card dashboard (gate per network/marketplace)

## FASE 5 — MARKETPLACE (§29/§30/§38)
- [x] COMMISSIONI (reconcile viaggi completati → Transaction ESCROW + COMMISSION, payout, ledger economico)
- [x] T7 — BORSA CARICHI (§29/§35): modello `MarketplaceLoad`, pubblicazione carichi (aziende verificate), ricerca viaggi/opportunità, filtri
- [x] T8 — MATCHING (§29): match origine/destinazione/data/mezzo/capacità tra carichi della borsa interna
- [x] T9 — SUBAPPALTO (§30): modello `Subcontract`, flusso VIAGGIO→ADMIN→DECIDE→MARKETPLACE→CERCA VETTORE→SELEZIONA→CONFERMA; solo suggerimenti, decisione dell'Admin; trip → `SUBAPPALTATO`
- [x] RICARICO (§31): prezzo, costo, ricarico e margine mostrati separatamente (campi Trip + report)

## FASE 6 — SMART RETURN (§32/§33/§34/§35)
- [x] T10 — SMART RETURN V1 DETERMINISTICA: match inverso (ORIGINE+DESTINAZIONE+DATA+MEZZO) sul network interno; metrica `KM A VUOTO PRIMA vs DOPO` e `RICAVO AGGIUNTIVO viaggi di ritorno` (§55)

## FASE 7 — ANALYTICS (§41/§42)
- [x] DATA base (Centro Dati: KPI, filtri, serie mensile, top clienti/autisti, export CSV)
- [x] T11 — ANALYTICS MEZZO (§42): km, costi, ricavi, margine per veicolo
- [x] T12 — ANALYTICS AUTISTA (§42): viaggi, km, puntualità, eventi per autista
- [x] T13 — RILEVAZIONE KM VIAGGIO: base dati km per viaggio (tabella `TripKm`, stima deterministica città; campagne per analytics/ESG/smart return)

## FASE 8 — REPUTAZIONE / COMMUNITY (§44/§45/§46/§47)
- [x] T14 — REVIEWS BLIND (§45): recensione bidirezionale VETTORE↔COMMITTENTE, pubblicazione dopo 7 giorni
- [x] T15 — BADGE (§46): badge automatici su criteri misurabili (ACCOUNT VERIFICATO, N+ VIAGGI, PUNTUALITÀ ELEVATA, DOCUMENTAZIONE OK)
- [x] T16 — AREE DI SOSTA (§47): community parcheggi (sicurezza, illuminazione, servizi, docce, ristorazione, feedback)
- [x] T17 — NETWORK (§65 fase8): directory aziende verificate nel network, profilo pubblico con reputazione

## FASE 9 — ESG (§48/§49)
- [x] T18 — ESG REPORT: km totali, km a vuoto, km evitati (da Smart Return), stima CO₂, report mensile/annuale, indicatori di efficienza

## FASE 10 — FEEDBACK COMMITTENTE / GO-LIVE (§8.4 paper deploy)
- [x] INCASSO RAPIDO "In arrivo" (Stripe Connect embedded finance): UI disabilitata su Economia+Borsa Carichi, lead gen idempotente `/api/finance/stripe-lead`, tabelle raw `StripeConnectProfile`+`StripeConnectLead` (DB predisposto), e2e `e2e-stripe-lead` verde — attivazione provider al §9 paper [n]
- [x] CHECKOUT STRIPE (abbonamenti BASE/PRO): `lib/plans.ts`, `GET /api/stripe/config`, `POST /api/stripe/checkout` (503 senza chiave live, riuso customer, session subscription), `StripeCheckoutCard` su dashboard; e2e `e2e-stripe-checkout` verde - chiave live NON salvata (da incollare nelle env di Vercel, #[9])
- [x] PAGINA ABBONAMENTI `/dashboard/abbonamenti` (FASE 4): piano attuale, stato TRIAL→ACTIVE, scadenza trial, account verificato (KYC), `StripeCheckoutCard` gestione, storico pagamenti (`Transaction` type SUBSCRIPTION), link in sidebar "Fatturazione"; Stripe live attivo (`configured:true`, price BASE 99€/PRO 199€ creati su Stripe: `price_1UIaW5DU9CV3V30wSX5p51Co`/`price_1UIaW6DU9CV3V30wo6nUCfQj`)
- [x] WEBHOOK KYC boilerplate: `app/api/webhooks/kyc/route.ts` (rate limit 60/min, validazione, firma in prod; mapping su `KycVerification` da collegare all attivazione provider)
- [x] WEBHOOK STRIPE LIVE VERIFICATO (FASE 4): endpoint `we_1UIaU2DU9CV3V30wUDS3PQ0C` su `https://www.truck-radar.it/api/webhooks/stripe`, scope Your account, eventi `customer.subscription.created/updated/deleted` + `invoice.paid` + subscription_schedule.*; gestione `customer.subscription.deleted` aggiunta (riporta a TRIAL); verifica firma HMAC reale end-to-end → `{"received":true}` 200; `subscriptionStatus` sincronizzato dallo stato Stripe (`active`→ACTIVE, altrimenti TRIAL)
- [x] Hardening webhook Stripe: `safeLog` + rate limit 100/min
- [x] T10.1 - LIVE FLEET MAP (ispirato MyGeotab): `lib/fleet.ts` (ultima posizione da `TrackingEvent` + viaggio corrente per veicolo), `GET /api/fleet/map`, `components/FleetMap.tsx` (Leaflet/OSM, zero API key), pagina `/dashboard/flotta` con stat mezzi+posizione+viaggio, refresh 15s, link nel menu
- [x] T10.3 - MANUTENZIONE PROGRAMMATA: tabella raw `VehicleMaintenance`, CRUD `/api/maintenance`, pagina `/dashboard/manutenzione` (tagliandi/km/scadenze/costi/fornitore/stato)
- [x] T10.4 - CHECK-LIST ISPEZIONE PWA: tabella raw `InspectionTemplate`+`VehicleInspection`, `/api/inspections` (template + esiti POST), pagina sede `/dashboard/ispezioni`, widget autista nel dettaglio viaggio (pre-partenza, esito OK/KO)
- [x] T10.5 - GEOFENCE/AREE: tabella raw `GeofenceArea`, CRUD `/api/geofence`, pagina `/dashboard/geofence` (centro/raggio/colore; alert in arrivo)
- [x] T10.6 - BOTTONI "Soon / In arrivo": `components/ComingSoonCard.tsx` riusabile, montato su dashboard home (Telemetria, Dashcam, Carte carburante/IFTA, Fleet Radar AI) - nessun provider richiesto per il deploy base

## FASE 11 — MIGLIORAMENTI DA OPEN-SOURCE (fleetops / aakvatech / eclipse-sdv)
- [x] F11.1 - CARBURANTE & CONSUMI (Fuel Log da fleetops/aakvatech): tabella raw `FuelLog` (litri/costo/odometro/pieno/luogo), `lib/fuel.ts` (stats km/l e €/km per mezzo), CRUD `GET/POST/DELETE /api/fuel` (403 autista in POST), pagina `/dashboard/carburante` (KPI + efficienza per mezzo + tabella), link nel menu Network, rimossa card "Carte carburante" dalle ComingSoon (modulo attivo)
- [x] F11.2 - TRACK REPLAY (da fleetops "replay movement"): `getVehicleTrack` in `lib/fleet.ts` (ultimo tracciato da `TrackingEvent`, ripiego sul trip più recente), `GET /api/fleet/track?vehicleId=`, bottone "▶ Riproduci percorso" nei popup della `FleetMap` (polyline + marker animato con setView), seed tracking demo (4 punti Roma→Milano) per il mezzo del tenant admin
- [x] F11.3 - MANUTENZIONE PREVENTIVA (da fleetops maintenance schedules): card "Prossime scadenze (30 giorni)" su `/dashboard/manutenzione` con kmProssimo/dataProssima entro soglia e alert in evidenza

## FASE 6B — SMART RETURN IBRIDO (§34, Priorità 2)
- [x] T19 — SMART RETURN IBRIDO Priorità 2 (API esterne): tabella raw `ExternalLoad` (carichi CERCO da piattaforme esterne, seed TimoCom/Teleroute su rotte inverse), connessioni `ApiConnection` tipo BORSA SYNCED per aziende demo, `hasBorsaConnections`/`findExternalReturnMatches` in `lib/marketplace.ts` (autorizzazione = client collegato a borsa esterna), fallback in `analyzeSmartReturn` quando rete interna senza match, `source` (INTERNO/ESTERNO) + `externalLoadId` su `SmartReturn`, `smartReturnLoadPrice`, POST `/api/smart-return` con `externalLoadId`, UI smart-return con badge "piattaforma esterna"; e2e `e2e-smart-return` esteso verde (match interno + fallback TimoCom, kmVuotiDopo=0, source ESTERNO); tsc/build puliti, deploy live

## KM ORIZZONTALI (§65)
- [x] Security/Privacy requisiti (§62): password hashing, sessioni, isolamento tenant, audit, upload sicuri
- [x] SECURITY HEADERS (§62): CSP, X-Frame-Options DENY, X-Content-Type-Options nosniff, Strict-Transport-Security preload, Referrer-Policy, Permissions-Policy in `next.config.ts` (verificati in produzione)
- [x] PRIVACY/LEGAL (§63): pagina `/privacy` strutturata (titolare, finalità, base giuridica, responsabili esterni, conservazione, diritti GDPR), nuova `/informativa-autisti` (tracking conducenti, art. 4 Statuto Lavoratori), `/termini`, `/cookie`, link nel footer home
- [n] Rotazione password DB Supabase — richiede azione utente (dashboard) — NON blocca il loop; se DB irraggiungibile → `[FAILED-PASSWORD-DB]` e avanti

## REQUISITI UTENTE — PIANI & ACCESSO
- [x] LIMITI PIANO (richiesta utente): TRIAL/KYC-L1 → solo funzioni base gestione flotta, sezioni premium solo visibili (badge PRO bloccato → `/dashboard/abbonamenti`); BASE → tutte le sezioni ma **15 mezzi** e **15 autisti**; PRO → **50 mezzi** e **50 autisti**; flotte più grandi → sistema customizzato. Implementato: `lib/plans.ts` (`getPlanLimits`, `hasFullSectionAccess`), sessione popolata in `auth.ts`, enforcement backend `POST /api/vehicles` (403 se limite raggiunto) e `POST /api/users` (403 autisti), sidebar `app/dashboard/layout.tsx` con contatori Mezzi/Autisti + link bloccato per sezioni premium, deploy Vercel live (promozione `2xok6yqbn`)
- [x] KYC L1 enum migration (DB): `scripts/ensure-raw-tables.ts` estende `KycDocType` con valori L1/L2 (`ALTER TYPE ... ADD VALUE IF NOT EXISTS`), eseguita sul DB Supabase
- [x] UPLOADTHING_TOKEN su Vercel: aggiunto env Production (base64 `{apiKey, appId, regions}`) — upload KYC doc ora funzionante (`GET /api/uploadthing` risponde 200 col router, niente più "Missing token")
- [x] E2E registrazione KYC L1 + limiti piano: `scripts/e2e-register-limits.ts` — **8/8 PASS** (registrazione con 2 doc L1, blocco senza doc, login, session con plan/limit, creazione veicolo/autista sotto limite, blocco 403 al 16° veicolo con `Limite piano raggiunto`)
- [x] Verifica finale: `npx tsc --noEmit` pulito, `npm run build` pulito, deploy Vercel production `6f5n7pc4u` Ready, `/register` 200 dinamico, `/api/uploadthing` 200, landing nuovo titolo live

## ASSENTI / BLOCCATI SU INPUT UTENTE
- [n] Logo: modello non può leggere `IMG_8382.jpeg` (immagine) — serve URL/base64/path nel repo dal proprietario per implementare; altrimenti resta placeholder Truck.
